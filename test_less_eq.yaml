# Тест операции <=
# Сравниваем значения в памяти[500] и памяти[501]

# Загружаем значения для сравнения
- read_mem: 500    # data[500] = 15
- read_mem: 501    # data[501] = 20

# Выполняем операцию <= (15 <= 20 = true)
- less_or_eq: 0    # аргумент игнорируется для этой операции

# Результат (1 = true) записываем в память
- write_mem: 600   # data[600] = 1